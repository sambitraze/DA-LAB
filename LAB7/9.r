#R program  for  A factory has 3 division and stocks 4 categories of products. An inventory table is updated for each division and for each product as they are received. 
# There are three independent suppliers of products to the factory: O Design a data format to represent each transaction. 
# O Write a program to take a transaction and update the inventory. O If the cost per item is also given write a program to calculate the total inventory values. 

factory = {
    "division": ["A", "B", "C"],
    "product": ["A", "B", "C", "D"],
    "quantity": [1, 2, 3, 4],
    "cost": [1, 2, 3, 4]
}

